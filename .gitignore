.instructions.yaml
#draft
#__draft__
*.pyc
__pycache__/
*.swp
.pytest_cache/

# Terraform
.terraform/
.terraform.lock.hcl
!infra/console/.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
backend.tf
terraform.tfvars

# Local CA certificates used only by Docker test builds behind TLS interception.
docker/certs/*.crt
.memsearch

# Agent scratch (local only — never commit)
WORKLOG.md
work-log
REVIEW-*.md
review/

# LLM kit: public/ is kit-synced and disposable. this_repo/ is this
# repo's overlay (CLAUDE.md / AGENTS.md / python skill sources) — keep it.
.shared-llm/public/
.shared-llm
AGENTS.md
CLAUDE.md
PLANS
.lavish
.claude
.claude/worktrees/
.impeccable/
.pi-skills
.pi-skills/
.pi-skills/*
.pi-lens
.pi-lens/
.pi-lens/*
